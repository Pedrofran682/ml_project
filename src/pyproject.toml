# pyproject.toml

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "ctscan-project"
version = "0.1.0"
authors = [
  { name="pmourafr" },
]
description = "A local package for my ct scan project."
requires-python = ">=3.13"